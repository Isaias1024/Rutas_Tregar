# El contexto del build es la raiz del monorepo (apps/worker depende de
# packages/shared via workspace:*, asi que el Dockerfile no puede limitarse
# a apps/worker solo). Todo lo de aqui abajo nunca hace falta dentro de la
# imagen.
**/node_modules
**/.next
**/.expo
**/.turbo
.git
drizzle
blueprints
tests/e2e
test-results
playwright-report
apps/mobile/android
apps/mobile/ios
*.log
.env
.env.*
!.env.example
